services:
  - type: web
    name: f1-predictor-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /health
    # Render injects PORT automatically; the Dockerfile CMD binds to it.
    # Set CORS_ORIGINS to the deployed frontend origin in the Render dashboard.
